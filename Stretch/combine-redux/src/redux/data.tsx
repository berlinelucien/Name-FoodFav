import { cakeAppState } from './types';
import { icecreamAppState } from './types';

export const cakeInitialState: cakeAppState = {
    numCake: 10
};

export const icecreamInitialState: icecreamAppState = {
    numIcecream: 12
};